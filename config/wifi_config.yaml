# 보드 진단/테스트용 사용자 설정.
# WiFi 접속 테스트에 쓸 자격증명을 담는다. 이 프로젝트에선 사용자 요청으로 이
# 파일을 commit 에 포함한다(자격증명 주의). 값은 실행 시 런타임에 읽혀 시리얼로
# 펌웨어에 주입된다(빌드 시 .bin 에 박지 않음 — config.yaml 만 바꾸면 재빌드 불필요).

# WiFi 연결 테스트에 사용할 AP (2.4GHz 대역 — ESP32-S3 는 5GHz 미지원).
wifi:
  ssid: "Skylife_STBWifi_2G_CB20"
  password: "12345678"
